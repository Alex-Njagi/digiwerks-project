import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    brand: {
      pastelPink: "#ffd1dc",
      pink: "#ff4da6",
      blue: "#4da6ff"
    }
  }
});

export default theme;